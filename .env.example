# VITE_GOOGLE_SHEET_WEBAPP_URL: Google Apps Script Web App URL — this is the app's
# only backend. Users can also set this at runtime via the Google Sheets Sync modal
# in the app UI (saved to localStorage), so setting it here is optional.
VITE_GOOGLE_SHEET_WEBAPP_URL=""

