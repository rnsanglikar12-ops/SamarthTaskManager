# The app's backend is Supabase (Postgres) — see supabase/migrations/ and
# src/utils/supabaseService.ts. Required in every environment.
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# --- Deprecated ---
# Google Apps Script Web App backend. Kept only as a dormant reference
# (src/utils/googleSheetsService.ts, scripts/apps-script-complete.gs) — the
# app no longer reads or uses this at runtime.
# VITE_GOOGLE_SHEET_WEBAPP_URL=""
